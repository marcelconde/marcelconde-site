backend:
  name: github
  repo: marcelconde/marcelconde-site
  branch: main
  base_url: https://auth.marcelconde.com.br
  auth_endpoint: auth

media_folder: "images/portfolio"
public_folder: "/images/portfolio"

collections:
  - name: "portfolio"
    label: "Portfólio"
    folder: "content/portfolio"
    create: true
    format: "json"
    slug: "{{slug}}"

    # ✅ cada item (álbum) faz upload pra própria pasta
    media_folder: "images/portfolio/{{slug}}"
    public_folder: "/images/portfolio/{{slug}}"

    fields:
      - { label: "Título", name: "title", widget: "string", required: true }
      - { label: "Slug", name: "slug", widget: "string", required: true, hint: "ex: pre-wedding (sem acento, sem espaço)" }
      - { label: "Categoria", name: "category", widget: "string", required: true }
      - { label: "Imagem de Capa", name: "thumbnail", widget: "image", required: true }
      - label: "Fotos do Álbum"
        name: "images"
        widget: "list"
        field: { label: "Foto", name: "image", widget: "image" }